# ATLAS — ตัวแปรสภาพแวดล้อมสำหรับ Frontend
# Copy ไฟล์นี้เป็น .env แล้วเติมค่าจริง

# Supabase (ต้องมีทั้งสองตัว)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Optional: Project ID (ใช้สำหรับ link/deploy)
# VITE_SUPABASE_PROJECT_ID=ebyelctqcdhjmqujeskx

# หมายเหตุ: LOVABLE_API_KEY ตั้งใน Supabase Dashboard เท่านั้น
# (Edge Functions → Secrets) — ไม่ใส่ใน .env
