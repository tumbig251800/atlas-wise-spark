# Session Summary — 2026-05-04

## งานที่ทำวันนี้

### 1. Restore ฟีเจอร์ AI ที่ปรึกษา & สร้างข้อสอบ
ฟีเจอร์หายไปจาก branch ปัจจุบัน (brave-torvalds-c346dc) เพราะ `Consultant.tsx` ไม่มีใน worktree นี้

**สิ่งที่เพิ่มเข้ามา:**
- `src/pages/Consultant.tsx` — copy จาก worktree `competent-kepler`
- `src/domain/consultantContext.ts` — dependency ของ Consultant
- `src/lib/contextValidator.ts` — dependency ของ Consultant
- `src/App.tsx` — เพิ่ม route `/consultant`
- `src/components/AppSidebar.tsx` — เพิ่มเมนู "AI ที่ปรึกษา & ข้อสอบ" (teacher + director)

### 2. Fix ai-exam-gen Edge Function
Edge function ใช้ environment variable และ model ผิด ทำให้ generate ข้อสอบไม่ได้เลย

| จุด | ก่อน | หลัง |
|-----|------|------|
| Secret key | `GEMINI_API_KEY` | `LOVABLE_API_KEY` |
| Gemini model | `gemini-2.0-flash` | `gemini-2.5-flash` |

Deploy ขึ้น Supabase เป็น version 27

### 3. Academic Term Filter
รองรับการเปิดเทอมใหม่ (เทอม 1/2569) ไม่ให้ข้อมูลสองเทอมปนกัน

**TeachingLog form:**
- Auto-compute `academic_term` จาก `teaching_date` ตอน submit
- สูตร: พ.ค.–ก.ย. → `{thaiYear}-1`, พ.ย.–มี.ค. → `{thaiYear}-2`
- ตัวอย่าง: วันที่ใน พ.ค. 2026 → `2569-1`

**Consultant page:**
- เพิ่ม dropdown เลือกภาคเรียนใน filter bar (แสดงเสมอเมื่อมีข้อมูล)
- Auto-select เทอมล่าสุดให้อัตโนมัติ
- เมื่อเปลี่ยน subject/grade/classroom จะ reset term selection
- Dialog สร้างข้อสอบ: แสดง badge "ภาคเรียน XXXX-X" บอกชัดว่าใช้ข้อมูลเทอมไหน
- AI chat และ exam gen ใช้เฉพาะ logs ของเทอมที่เลือก

---

## Commits วันนี้

| Commit | รายละเอียด |
|--------|-----------|
| `719d9b7` | feat(consultant): restore AI consultant & exam gen feature |
| `98ccdcd` | feat: auto academic_term on log + term filter on Consultant |
| `bd812db` | fix(consultant): always show term selector + show term badge in exam dialog |

---

## สถาปัตยกรรมฟีเจอร์สร้างข้อสอบ

```
Consultant.tsx
  ├── Filter: subject / gradeLevel / classroom / academic_term
  ├── useContextFirstTeachingLogs() → ดึง logs จาก Supabase
  ├── filteredLogs = allLogs.filter(academic_term == selectedTerm)
  │
  ├── AI Chat (พีท ร่างทอง)
  │   └── POST /ai-chat  audience="teacher"  context=buildContextWithCitation(filteredLogs)
  │
  └── Exam Gen Dialog
      ├── Step 1: เลือก learning_unit จาก filteredLogs
      └── Step 2: POST /ai-exam-gen
              body: { gradeLevel, classroom, subject, unit, context (REF-X format) }
              model: gemini-2.5-flash via LOVABLE_API_KEY
              prompt: ATLAS Bloom's Taxonomy 10 ข้อ (K/P/A-Gap)
```

---

## งานค้างสำหรับครั้งหน้า

### Export Word ในฟีเจอร์สร้างข้อสอบ (มอบให้ Cursor)
- เพิ่มปุ่ม "ดาวน์โหลด .docx" ใน exam dialog ข้างปุ่มคัดลอก
- Reuse `ExportDocButton` component ที่มีอยู่แล้ว (ใช้ library `docx` ที่ bundle ไว้แล้ว)
- ชื่อไฟล์: `ข้อสอบ-{subject}-{gradeLevel}-{classroom}-{unit}-{term}.docx`
- ไม่ต้องทำตาราง MCQ สวย — plain text + heading ก็พอสำหรับ MVP
