project_id = "ebyelctqcdhjmqujeskx"

# Phase 2.2: verify_jwt=true — ป้องกันการเรียกโดยไม่มี JWT (401 ถ้าไม่มี Authorization)
[functions.ai-chat]
verify_jwt = false

[functions.ai-summary]
verify_jwt = false

[functions.ai-lesson-plan]
verify_jwt = false

[functions.atlas-diagnostic]
verify_jwt = false

[functions.ai-exam-gen]
verify_jwt = false

# atlas-mcp / kindergarten-mcp: removed from production 2026-08-03 (resource
# exhaustion — see docs/SECURITY-AUDIT-2026-08-03-open-findings.md §3). Source
# archived under docs/archived-functions/. Deliberately NOT declared here so a
# blind `functions deploy` can't put them back on production.

# atlas-mastery-watch: auth handled internally via requireAtlasUser
[functions.atlas-mastery-watch]
verify_jwt = false

# atlas-integrity-recheck: auth handled internally via requireAtlasUser
[functions.atlas-integrity-recheck]
verify_jwt = false
